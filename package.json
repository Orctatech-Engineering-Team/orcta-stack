{
  "name": "orcta-stack",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "./scripts/setup.sh",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm -r --filter ./apps/frontend build",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "clean": "rm -rf apps/*/dist packages/*/dist node_modules/.cache"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7"
  },
  "dependencies": {},
  "engines": {
    "deno": ">=2.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
