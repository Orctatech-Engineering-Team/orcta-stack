NODE_ENV=test

# Use the same Postgres instance as development; tests must clean up after themselves.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/orcta_dev

# 32-char minimum secret for better-auth — only used in tests.
BETTER_AUTH_SECRET=test-only-secret-do-not-use-in-production-1234

BETTER_AUTH_URL=http://localhost:9999
SERVER_URL=http://localhost:9999
FRONTEND_URL=http://localhost:5173
